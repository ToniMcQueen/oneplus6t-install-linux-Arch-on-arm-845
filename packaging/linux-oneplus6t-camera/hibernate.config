# S4/hibernate lab support.
#
# The PMOS SDM845 base config has ARCH_HIBERNATION_POSSIBLE=y and SWAP=y, but
# CONFIG_HIBERNATION is disabled. Without this, /sys/power/state does not expose
# "disk", so no swapfile/resume setup can make S4 work.
CONFIG_HIBERNATION=y
